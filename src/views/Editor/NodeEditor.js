import React from 'react';
import { inject, observer } from 'mobx-react';

const NodeEditor = observer(() => (
  <div className="node-editor-sidebar">
    Node Edititing go here
  </div>
));

export default NodeEditor;
